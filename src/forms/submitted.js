import React from "react";
import Grid from "../components/grid";
import { Title } from "./title";

export default () => (
  <Grid>
    <Title text={"We'll be in touch soon!"}>
      We're just starting out, so a human reviews every match for quality. This
      makes the process take between 24 and 48 hours, which is a bit slower than
      we'd like, but rest assured we'll get in touch with you soon.
    </Title>
  </Grid>
);
